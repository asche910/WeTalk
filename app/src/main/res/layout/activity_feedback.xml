<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include layout="@layout/toolbar_universe"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/edit_feedback"
        android:gravity="center"
        android:layout_marginBottom="30dp"
        android:text="请输入产品的问题或您的宝贵意见："
        android:textSize="18sp"
        android:textColor="?attr/textNormal"/>

    <EditText
        android:id="@+id/edit_feedback"
        android:layout_width="300dp"
        android:layout_height="150dp"
        android:layout_marginLeft="50dp"
        android:layout_marginRight="50dp"
        android:layout_centerInParent="true"
        android:padding="10dp"
        android:gravity="start"
        android:hint="请输入您的宝贵意见..."
        android:background="@drawable/bg_edit_report"
        />

    <Button
        android:id="@+id/btn_feedback"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/edit_feedback"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:text="提 交"
        android:textColor="@color/white"
        android:textSize="18sp"
        android:background="@drawable/bg_btn_blue"/>

</RelativeLayout>